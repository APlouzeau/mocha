import 'package:flutter/material.dart';

class PostFocus extends StatelessWidget {
  const PostFocus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Bienvenue sur la page d\'un post et ses commentaires !')),
    );
  }
}